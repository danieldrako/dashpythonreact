import React from 'react'

function GeneralJsonComponent({ data }) {
  return (
    <div>
      <h2>General JSON Component</h2>
      {/* <pre>{JSON.stringify(data, null, 2)}</pre> */}
    </div>
  );
}

export default GeneralJsonComponent